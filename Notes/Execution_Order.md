

# Query Execution

### FROM TABLE
whenever we are executing a query First "FROM" is executed cause we need data to do soemthing with it.

### Columns
select column1, column2
this is executed at last mostly cause we are just taking columns we want to shwo from finalized data.


# EXECUTION ORDER

FROM
JOIN
WHERE
GROUP BY
HAVING
SELECT
ORDER BY
LIMIT
