

## GET ALL THE DATA

if we need to do some filtering from multiple tables
FIrst we need to know which data to get and which join can give us that result
after that apply query on that.

---

## SQL Writing Order 
SELECT
FROM
JOIN
WHERE
GROUP BY
HAVING
ORDER BY
LIMIT